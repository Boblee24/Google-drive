const Upload = () => {
    const uploadFile = () => {

    }
  return (

    <div>
        <input type="file" />
        <button onClick={uploadFile}>Upload file</button>
    </div>
  )
}

export default Upload